import React from 'react';
import ReactDOM from 'react-dom/client';
import AddTicket from './components/main';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <AddTicket/>
);
